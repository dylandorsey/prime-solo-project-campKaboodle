import React from 'react';
import { NotInterested } from '@material-ui/icons';

const ButtonLeaveTrip = props => (
        <NotInterested onClick={props.onClick} />
);

export default ButtonLeaveTrip;